buffer = {
    host = "localhost"
    port = 5672
    queue_name = "buffer"
    user = "guest"
    password = "guest"
}

sources = [
    {
        type = "random"
        interval = 2 seconds
        metric = ["a", "b", "c"]
        tagset = {}
    }
]

sinks = [
    { type = "stdout/json" },
    {
        type = "mqtt/pb"
        host = "localhost"
        port = 1883
        client_id = "collector 1"
        channel = "timeseries"
        security = {
            type = "password"
            login = "steve"
            password = "password"
        }
    }
]

